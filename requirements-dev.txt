-r requirements.txt
pytest>=8.0
pytest-django>=4.8
pytest-cov>=5.0
factory-boy>=3.3
# Pinned: ruff 0.16 widened its default rule set, so an unpinned lint job
# starts failing on untouched code the day a new version ships.
ruff>=0.15,<0.16
black>=24.0
